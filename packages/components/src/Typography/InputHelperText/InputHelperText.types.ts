export interface InputHelperTextBaseProps {
  /** Text or array of text containing info about the input, can be used for errors */
  text?: string | string[];
  /** Children are not allowed */
  children?: null;
}
