import { MetaRecord } from "nextra";

const meta: MetaRecord = {
  index: "Getting Started",
  install: "Installation",
  "###": { type: "separator", title: "Collections" },
  projects: "Projects",
};
export default meta;
